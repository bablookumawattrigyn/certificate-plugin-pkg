import React from 'react';
import ReactDOM from 'react-dom/client';
import CertificatePlugin from './index.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CertificatePlugin />
  </React.StrictMode>,
);
